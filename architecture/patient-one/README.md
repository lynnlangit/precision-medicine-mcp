# PatientOne Architecture Concept

PatientOne demonstrates the set of spatial and multi omics MCP servers in action.  
The demo uses synthetic patient data.  The use case is a patient with Stage IV Ovarian Cancer.  

Shown below is a conceptual diagram (MCP servers shown consolidated by function).  
